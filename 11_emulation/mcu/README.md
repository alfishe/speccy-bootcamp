[← Emulation](../README.md) · [MCU Emulation](README.md)

# Emulation — MCU Chip Emulation

This directory covers replacing vintage silicon with modern microcontrollers: Z80 on MCU, ULA on MCU, FDC, PSG, keyboard, video adapters, SD interfaces, and design patterns. MCU-based chip emulation is a growing trend in retro-computing — modern MCUs (especially the RP2040 with its PIO blocks) can replace ageing Z80s, ULAs, AY chips, and floppy controllers at lower cost, lower power, and with additional features like in-circuit debugging and tracing.

For FPGA-based chip emulation, see the [fpga](../fpga/) directory. For software emulators running on PCs, see the [software](../software/) directory.

## Articles

| # | File | Topic |
|---|---|---|
| 1 | [mcu_z80.md](mcu_z80.md) | **Z80 on a Microcontroller** — replacing the Z80 CPU with an RP2040/ESP32/STM32. Why MCU (component availability, power consumption, reliability, additional features like debugging/tracing/integrated peripherals, compatibility layer presenting the full Z80 bus interface). Host MCU choices: RP2040 (Raspberry Pi Pico, dual Cortex-M0+ at 133 MHz, **PIO blocks** for cycle-precise bus I/O, 30 GPIOs — the optimal choice), ESP32 (Xtensa/RISC-V at 240 MHz with Wi-Fi/Bluetooth), STM32 (F407 with hardware FMC), Arduino AVR (too slow), Teensy (high-performance). **Bus interface design**: address/data/control signal mapping (16+8+13 = 37 signals in 40-pin Z80 package, pin count problem solved with external latches/SPI expanders/CPLDs), voltage level translation (74HCT245 buffers for 3.3V MCU ↔ 5V bus, HCT vs HC thresholds). **Timing requirements**: Z80 nanosecond-level bus timing, realising it on a 133 MHz MCU (38 cycles per T-state), instruction-stepped vs cycle-stepped emulation (cycle-stepped needed for cycle-exact accuracy). **Z80 instruction emulation**: documented + undocumented instructions (`SLL`/`SLI`, `LD A,I`/`LD A,R` flags = IFF2, `OUT (C),0` NMOS writes 0 vs CMOS writes 0xFF, `BIT n,(HL)` X/Y flags), cycle counts, implementation techniques (direct interpretation, threaded code, JIT). Existing projects (PicoROM, PicoZ80, Pico Spectrum, libz80, z80ex). Integration with real Spectrum hardware (pinout adapters, level shifters, crystal oscillator). MCU Z80 vs Real Z80 vs FPGA T80 decision matrix. FAQ (RP2040 speed, undocumented instructions, IM 0/1/2, 3.3V on 5V bus, ZEXALL pass but demos fail, MCU speed requirement ~30× Z80 clock, integration on single RP2040, bus capacitance). Summary, references, cross-references |
| 2 | [mcu_ula.md](mcu_ula.md) | **ULA on a Microcontroller** — replacing the Ferranti ULA (most failure-prone original Spectrum component, no replacements available since Ferranti sold to Plessey/scrapped tooling) with an MCU. Why replace (ULA failure symptoms — no video, garbage screen, no keyboard, no sound, random crashes; Chris Smith's reverse-engineering book enables modern recreation; RP2040 PIO optimal for cycle-precise video). **ULA functions**: video generation (video address counter walking through display RAM, pixel shift register, attribute application via colour encoder, composite PAL signal with HSYNC/VSYNC/blanking/colour burst, BORDER mixing); memory arbitration (ULA shares RAM with CPU, asserts WAIT_n during active display producing contended memory timing in `0x4000–0x7FFF`); I/O ports (`0xFE` write for beeper/MIC/EAR/BORDER colour, `0xFE` read for keyboard matrix row via `A[8:15]`, `0xFF` floating bus); INT generation at scanline 64 every 20 ms; clock generation (14 MHz master / 4 = 3.5 MHz CPU); beeper (1-bit DAC via port `0xFE` bit 4). **Video generation on RP2040 PIO**: pixel timing (7 MHz pixel clock, 448 pixels/scanline × 311 scanlines); PIO program for composite video (load pixel data, shift out 8 pixels/byte × 32 bytes/scanline, HSYNC 4 µs, VSYNC sequence per PAL); composite signal requirements (HSYNC, VSYNC with PAL equalisation pulses, 4.43 MHz colour burst for compat, blanking levels); VGA alternative (separate HSYNC/VSYNC, RGB via resistor DAC, simpler than PAL); HDMI via Pico DVI (bit-banged DVI through PIO). **Contention emulation** via state machine: given video position + CPU accessing contended + cycle in 4-T-state character, decide WAIT_n; pseudocode for contention logic; integration with cycle-stepped Z80 emulator. **Floating bus emulation**: track ULA's current video fetch byte, return on `0xFF` read during contention cycle, otherwise `0xFF`. **I/O port tables** (port `0xFE` write bits 0-2 BORDER, 3 EAR, 4 MIC/beeper; port `0xFE` read bits 0-4 keyboard, 6 EAR). **Complete ULA replacement**: video address counter with bit shuffling (C code example with Y7-Y0 → addr 13 12 7 6 5 10 9 8 4-0); INT generation via timer; clock generation; beeper; 128K/+2/+3 considerations (memory banking via `0x7FFD`, AY-3-8912, different contention scheme, different video timing); integration with Z80/RAM/ROM/AY/keyboard/SD on single RP2040. Existing projects (Pico Spectrum, Yazoo's, PicoVGA, Pico DVI, SpecHMI). **Comparison with FPGA ULA recreation** (Harlequin/Sizif-512): MCU advantages (easier development C/C++, lower cost £1 vs £3-10, faster iteration reflash vs resynthesis, integrated peripherals, larger community) vs FPGA advantages (native parallelism, lower jitter, authentic bus signals, established solutions). Trade-off summary table. FAQ (RP2040 PAL composite via resistor DAC, cycle-exact contention with cycle-stepped, floating bus implementation, ULA+Z80 on single RP2040, composite vs VGA/HDMI trade-off, video memory buffer necessity, contention accuracy requirements for 95% software vs demoscene, 128K banking-aware contention). Summary (5 requirements: PIO-driven video, contention state machine, floating bus, I/O ports, INT generation). References (Smith's book, RP2040 datasheet, Pico Spectrum/PicoVGA/Pico DVI projects, PAL spec, Sensible tests). Cross-references |
| 3 | [mcu_fdc_vg93.md](mcu_fdc_vg93.md) | **KR1818VG93 / WD1793 FDC on a Microcontroller** — replacing the Beta 128 disk interface's floppy disk controller with an MCU (typically STM32). The **WD1793** (and Russian **KR1818VG93** clone) handles MFM/FM encoding, track seeking, sector read/write, CRC, index pulse detection; presents a 5-register interface (Command, Status, Track, Sector, Data). **Beta 128 disk interface**: FDC + TR-DOS ROM + 34-pin floppy cable + address decoding at I/O ports `0x1F`/`0x3F`/`0x5F`/`0x7F`. **TR-DOS** by Mikhail Shumakov — file ops, BASIC extension (`CAT`, `LOAD *"file"`, `SAVE *"file"`), thin layer over Beta 128 hardware. Why replace: failing original chips (30+ years old — read errors, write failures, seek failures, complete failures), media degradation (magnetic domain decay, physical deterioration, fungal growth, mechanical wear), convenience of SD-card loading, authentic form factor preservation for Pentagon/Scorpion/ATM Turbo owners. **VG93Em-STM32 reference implementation**: STM32F103 Blue Pill or STM32F407, microSD via SPI, 40-pin DIP connector for Beta 128 socket, status LEDs, optional config jumpers; firmware implements VG93 register interface and full command set (restore, seek, step/step-in/step-out, read sector, write sector, read address, read/write track, force interrupt); pseudocode C example showing command dispatch. **Disk image formats**: `.trd` (standard TR-DOS 640 KB = 80 tracks × 16 sectors × 256 bytes), `.scl` (sector-based with different header), `.fdi` (raw MFM with copy protection preservation), `.imd` (ImageDisk format), `.opd`/`.dsk`. **Seek emulation** with settle time delay matching real chip. **Sector read/write**: DRQ bit set, host reads/writes Data register, 256 bytes per sector. **Status register bits**: Motor On, Write Protect, Spin-Up/Ready, Record Not Found/Seek Error, CRC Error, Track 0, Index Pulse, Busy. **Timing considerations**: real FDC operates at 300 RPM = 200 ms/revolution = 12.5 ms/sector; DRQ every 31 µs; index pulse periodic. **Alternative implementations**: ESP32 (240 MHz with Wi-Fi for network disk serving), RP2040 (with PIO for precise timing), FPGA (MiSTer/ZX-Uno with HDL VG93), Gotek floppy emulators (replace drive not FDC, with HxC firmware supporting many formats via USB stick). Comparison table across implementations (cost, difficulty, timing accuracy, best-for use case). **Integration with real hardware**: drop-in chip replacement on small PCB with 40-pin DIP + external SD slot, or external module cabling; pinout adapter + level shifters + SD card slot + configuration interface. **Copy protection considerations**: non-standard sector sizes (128/1024/2048 bytes), non-standard sector IDs, hidden tracks (81/82), weak/fuzzy bits producing different reads, timing-based protection measuring FDC event intervals — `.fdi`/`.imd` formats help but advanced protection remains difficult. FAQ (STM32 choices F103 vs F407, TR-DOS software compat at 95%+, image selection via buttons/OLED/config file, write-back support, Beta 128 ROM still needed, SPI vs SDIO trade-off, timing accuracy within microseconds). Summary (4 elements: drop-in chip replacement, SD-card storage, register-accurate emulation, timing-aware DRQ/status). References (WD1793 datasheet, VG93 Russian datasheet, VG93Em-STM32 project, TR-DOS documentation, Beta 128 spec, HxC Gotek, disk image format specs). Cross-references |
| 4 | [mcu_psg_ay.md](mcu_psg_ay.md) | **AY-3-8912 PSG on a Microcontroller** — replacing the AY-3-8910/8912/8913 sound chip (or Yamaha YM2149, Russian KR1518VG94) with an MCU. Chip family (AY-3-8910 40-pin with 2 I/O ports, AY-3-8912 28-pin with 1 I/O port used in Spectrum 128K/+2/+3, AY-3-8913 24-pin no I/O ports, YM2149 Yamaha pin-compatible clone with better DACs, KR1518VG94 Russian clone). **16 registers** table (R0-R5 tone A/B/C period 12-bit, R6 noise period 5-bit, R7 enable tone/noise per channel + I/O, R8-R10 channel A/B/C amplitude 5-bit, R11-R12 envelope period 16-bit, R13 envelope shape 4-bit, R14-R15 I/O ports). 2-step host protocol (write register number to address latch at port `0xFFFD`, write data to data register at port `0xBFFD`). **Sound generation**: square wave per channel (12-bit period, freq = clock/(16×period)), noise generator (5-bit period, 17-bit LFSR), envelope generator (16-bit period, 4-bit shape selecting 16 attack/decay/sustain/release combinations), 4-bit logarithmic DAC per channel producing characteristic beepy sound. PSG clock (Spectrum 128K = 1.7734 MHz = 3.5469 MHz / 2, Pentagon = 1.75 MHz with different pitch for same registers, YM2149 sometimes 1.75 or 2 MHz). Why replace: component failure (missing channels, noise generator failure, envelope failure, I/O port failure, total failure), audio quality enhancement (higher-resolution DACs 8/16-bit linear, linear interpolation, stereo output, digital filtering), stereo output (ABC/ACB/MON conventions, AY music file metadata), multiple AYs (TurboSound 6-channel with ports `0xFFFD`/`0xBFFFD` + `0x3FFD`/`0x5FFD`). **Implementation**: register emulation (ay_state_t struct), tone generation (counter with 16 PSG cycles per step), noise generator with LFSR feedback from bits 0 and 3, envelope generator with 32-step patterns per shape, audio mixer combining 3 channels with amplitude × tone/noise source. **Audio output**: sample rate 44.1/48 kHz; DAC options (MCU built-in 12-bit, PWM via GPIO + RC filter, external I2S DAC PCM5102 for 24-bit, multiple resistor R-2R ladder). **Stereo output** code example (ABC: A left, B centre, C right). **Multiple AYs** via separate state structs with port-based dispatch. Existing projects (AY-3-8912 STM32 emulator, emu2149 by Vincent Sanders, ym2149_emul, Pico AY with stereo, Schrödinger's AY). **AY file players** (AY-emul by Shcheglov, zxtune, ChipSeeR). **Integration with real hardware**: drop-in 28-pin DIP chip replacement + level shifters + audio output, or external audio module. **Comparison with FPGA PSG** (MCU advantages: easier C/C++ dev, lower cost, audio quality flexibility, multi-AY trivial, stereo flexibility; FPGA advantages: native parallelism, lower CPU overhead, established HDL cores). FAQ (AY vs YM2149 differences, log 4-bit amplitude scale for authentic sound, sample rate 44.1 kHz sufficient, AY I/O port emulation for Kempston mouse, PSG clock config per host, audio effects like reverb, AY file players without real Spectrum). Summary (5 benefits: eliminates failure, improves quality, adds stereo, supports TurboSound, maintains software compat). References (GI datasheet, Yamaha YM2149, emu2149, AY-emul, AY/YM file format specs, 128K service manual). Cross-references |
| 5 | [mcu_keyboard.md](mcu_keyboard.md) | **Keyboard Controller on a Microcontroller** — replacing the Spectrum's membrane keyboard (or 40-year-old failed membrane) with a modern PS/2 or USB keyboard via an MCU. **8×8 keyboard matrix** (8 row lines from `A[8:15]`, 5 column inputs at port `0xFE` bits 0-4 — scan by setting bit low in high address byte, read 5 column bits from low byte). Address mapping table for all 8 rows with all 40 keys (row 0 CAPS/Z/X/C/V through row 7 SPACE/SYM SHIFT/M/N/B). Extended 128K/+2/+3 keyboard (numeric keypad, cursor keys via additional rows). **Limitations of membrane keyboard** (no N-key rollover without diode isolation causing 3-key ghosting, slow membrane response, membrane degradation, poor tactile feedback). Why replace with MCU (membrane failure — dead/phantom/slow/stuck keys, ergonomic improvement — proper feedback/N-key rollover/function keys/numeric keypad/cursor keys, joystick integration via Kempston/Sinclair/Fuller/Protek, mouse integration via Kempston mouse). **MCU choices**: RP2040 (dual Cortex-M0+ at 133 MHz, PIO blocks, 30 GPIOs, USB host, ~£1 — optimal), ESP32 (240 MHz, Wi-Fi/Bluetooth, fewer GPIOs), STM32 (F103 Blue Pill or F407, 5V-tolerant GPIOs on some models), Arduino ATmega32U4 (Leonardo/Micro with native USB, basis for many DIY adapters). **Hardware connection to the Spectrum**: membrane connector replacement (invasive — 8 row + 5 column ribbon cables directly driven by MCU), joystick port (non-invasive but joystick-only), expansion port (intercepts I/O reads to port `0xFE`, fully non-invasive — the standard approach). **PS/2 keyboard input**: PS/2 protocol (clock + data, 11 bits/byte, make codes and `0xF0` break codes); scan code to ZX matrix translation via lookup table (C code with `ps2_to_zx[256]` indexed by scan code, 64-bit keyboard_state bit per matrix key, handle_make_code/handle_break_code). CAPS SHIFT vs SYMBOL SHIFT handling, layout variations (US/UK/QWERTZ/AZERTY). **USB keyboard input**: USB host MCUs (RP2040 with TinyUSB, ESP32 S2/S3 with OHCI, STM32 OTG, Arduino + USB Host Shield). USB HID protocol (8-byte reports: modifier byte + reserved + 6 keycodes), HID keycode translation table, full N-key rollover. **Joystick emulation**: Kempston (port `0x1F` bits 0-4 right/left/down/up/fire with C code), Sinclair 1 (keys 6-0) and Sinclair 2 (keys 1-5) via keyboard matrix, Fuller (port `0x7F` bits 0-3 + bit 6 fire), Protek/AGF (port `0xDF`). Gamepad mapping (D-pad to direction, A to fire, B to up+fire for platformer jump, Start to ENTER, Select to SPACE). **Kempston mouse emulation** (ports `0xFBDF`/`0xFFDF`/`0xFADF`/`0xBFDF` for X/Y/buttons, 8-bit counters updating from PS/2 or USB mouse deltas, button bits 0/1/2). **Existing projects**: ZXHIDKeyboard (RP2040/STM32 expansion port USB keyboard with multi-layout + Kempston mouse + function key shortcuts), ZXKey (Arduino ATmega32U4 membrane replacement for PS/2), ZXKB (ATmega328P Nano with PS/2 + Kempston port), RetroBrew adapters, ESP32 custom adapters with Bluetooth. Comparison table of approaches (Arduino membrane replacement, RP2040 expansion port, STM32 expansion port, ESP32 Bluetooth). **Integration with real hardware**: membrane connector approach (5 steps, invasive), expansion port approach (4 steps, non-invasive), modern recreation approach (keyboard scan in MCU firmware, no adapter needed). FAQ (PS/2 vs USB trade-off, multiple keyboards via OR'd states, F1-F12 mapping to Spectrum actions like NMI/reset/snapshot/tape, Caps Lock LED via MCU, wireless keyboards via ESP32 Bluetooth or USB receiver, keyboard scan speed 50 Hz with microsecond MCU response, Cyrillic layout for Russian Spectrums). Summary (5 functions: receive input, translate to matrix, drive matrix or intercept scan, optional joystick/mouse, additional features like function keys and wireless). References (Chris Smith's ULA book, Spectrum 48K service manual, PS/2 protocol by Chapweske, USB HID Usage Tables, RP2040 datasheet, TinyUSB library, ZXHIDKeyboard/ZXKey projects, Kempston joystick/mouse docs). Cross-references |
| 6 | [mcu_video_adapter.md](mcu_video_adapter.md) | **Video Adapter on a Microcontroller** — converting the Spectrum's composite PAL or RGB video output to modern formats (VGA, HDMI, DVI) via an MCU, with upscaling and CRT effects. Why need adapter (modern displays don't like Spectrum signals — composite blurry, RGB-on-SCART increasingly rare, 50 Hz refresh conflicts with 60 Hz monitors, 256×192 resolution tiny on modern displays, CRT artifacts lost on flat-panels). Adapter role (receive Spectrum video via signal intercept/video memory read/emulator state, convert to VGA/HDMI/DVI, upscale 256×192 to modern resolution, add optional CRT effects like scanlines/phosphor mask/bloom). **Video output options**: Composite PAL recreated via RP2040 PIO + resistor DAC (authentic but low quality, for original CRT TVs); VGA (analog RGB 0–0.7V + TTL HSYNC/VSYNC, accepts wide range of resolutions and refresh rates, RP2040 + 3 resistors per colour for 3-bit/512 colours or ADV7125 external DAC for 24-bit); HDMI/DVI (digital, high data rate 25 MHz pixel clock for 640×480 = 250 Mbps per channel, via bit-banged DVI with RP2040 PIO per Pico DVI project OR external HDMI encoder like ADV7513/TFP410 supporting 1080p + audio); DisplayPort (rare, complex encoding). **VGA output on RP2040**: hardware (3-bit colour per channel via 9 resistors, HSYNC/VSYNC direct GPIO, VGA 15-pin DSUB; or ADV7125 for 8-bit per channel); PIO program (pixel loop shifting one pixel/cycle, HSYNC at end of scanline, VSYNC at end of frame, pixel data via DMA from frame buffer); **PicoVGA library** by Miroslav Nemecek (defines standard modes 320×240/640×480/800×600, frame buffer in RP2040 RAM, handles PIO + DMA setup, 8-bit palettised 256 colours or 4-bit 16 colours, primitives for pixels/lines/rectangles/text); VGA timing 640×480 @ 60 Hz (pixel clock 25.175 MHz, HSYNC 96 pixels low 3.8 µs + 16 back porch + 480 active + 16 front porch = 800 total 31.77 µs, VSYNC 2 lines low + 33 back porch + 480 active + 10 front porch = 525 total, frame rate 59.94 Hz; custom mode for 50 Hz). **HDMI via Pico DVI** by Luke Wren (three TMDS channels for RGB + fourth for clock, three PIO state machines shift out 10-bit TMDS symbols at pixel clock rate, differential GPIO pairs with 100-ohm impedance resistor network; limits max 640×480 or 800×600 @ 60 Hz, 264 KB RAM tight, no audio since PIO fully occupied). **External HDMI encoder ADV7513** (~£10, supports up to 1920×1080 @ 60 Hz 1080p, audio via I2S, HDCP, standard HDMI connector). **Upscaling algorithms**: Nearest neighbour integer scaling (256×192 × N, fast/sharp/no artefacts, may not fit standard resolutions, common in retro computing); Bilinear filtering (smooths but destroys pixel art, rarely used); Scanline interpolation (256×384 alternating pixel/scanline rows); Integer scaling with aspect ratio correction (Spectrum pixels wider than tall — 256×192 is 4:3 not 16:9, integer scaling can't preserve exact aspect ratio, options: square pixels wrong aspect, non-integer vertical scaling, or letterboxing). **Scanline generation** (darken every other row by 50%, C code pseudocode; phosphor mask vertical colour subpixel stripes rarely on MCU; bloom/glow simulated via 3×3 box blur on bright pixels rarely on MCU). **Receiving Spectrum's video**: intercepting analog video signal via ADC scan converter (complex); reading video memory from integrated MCU ULA replacement (simplest, used by Pico Spectrum); frame grabber via SPI/parallel; software emulator internal state. **Existing projects**: PicoVGA library, Pico DVI project, RGB-to-HDMI (Pi Zero scan converter by Ian Stocks and David Banks, plus RP2040 variant), Retroleum SMARTi (VGA from 48K), ZX-HD (HDMI for 48K/128K), Spectra (RGB video interface). Comparison table of output formats (composite PAL/VGA resistor DAC/VGA ADV7125/HDMI Pico DVI/HDMI ADV7513/RGB-to-HDMI Pi Zero with cost/difficulty/quality/best-for). **Integration with original hardware**: external adapter (small box taking composite/RGB/edge connector, non-invasive); integrated adapter (MCU Spectrum generates video directly, video output circuit built in). FAQ (minimum resolution 640×480 for 2× scale with letterboxing, 768×576 for pixel-perfect 3×; audio on HDMI only via ADV7513 not Pico DVI; monitor refusing 50 Hz sync — use European monitor or output 60 Hz with frame interpolation; attribute clash preserved as authentic; screenshot capture via SD card; video output latency negligible for RP2040 direct, 1-2 frames for external HDMI encoders). Summary (5 functions: receive Spectrum video, upscale to modern resolution, generate VGA/HDMI/DVI signal, optional CRT effects, aspect ratio handling). References (PicoVGA by Nemecek, Pico DVI by Wren, RGB-to-HDMI project, ADV7513/ADV7125 datasheets, VGA timing docs, DVI spec, RP2040 datasheet, Retroleum SMARTi/ZX-HD/Spectra adapters, Chris Smith's ULA book). Cross-references |
| 7 | [mcu_sd_interface.md](mcu_sd_interface.md) | **SD Card Interface on a Microcontroller** — modern mass storage upgrade for the Spectrum, replacing cassette tape and floppy disk with SD cards. Why SD (capacity — thousands of programs on one card, entire WoS archive fits in a few GB; speed — 10–25 MB/s via SPI vs ~62 KB/s floppy, 30s floppy load becomes <1s; reliability — solid-state no moving parts 100k+ writes/sector; cost — 4 GB SD card ~£2-3). **SD card basics**: families (SDSC up to 2 GB original spec, **SDHC 4–32 GB** most common today using FAT32, SDXC 64 GB–2 TB requiring exFAT, SDUC 2 TB+ very new); form factors (Standard SD, miniSD rare, **microSD** most common in retro adapters); protocols (**SPI mode** 4 signals CS/SCK/MOSI/MISO simpler supported by virtually all MCUs at 12-25 MHz giving 1.5-3 MB/s — preferred for retro; SDIO mode 4 or 8 data lines faster but needs MCU hardware support). **Hardware connection**: SPI mode pinout (DAT2 unused pulled high, CD/DAT3=CS, CMD=MOSI, VDD 3.3V, CLK=SCK, VSS GND, DAT0=MISO, DAT1 unused pulled high); **level shifting** (5V Arduino needs resistor dividers on MOSI/SCK/CS, MISO direct since 3.3V is TTL high; 3.3V MCU like RP2040/ESP32/STM32 connects directly); power supply 3.3V at up to 100 mA during writes — check regulator current rating; card detect (CD) switch mechanical for hot-swap; write protect (WP) on full-size SD only ignored by most adapters. **SD card SPI protocol**: initialization sequence (power-up delay 1 ms; slow clock 100-400 kHz; 80 dummy clocks with CS high; CMD0 RESET switches to SPI mode expecting R1=0x01 idle; CMD8 SEND_IF_COND checks voltage; CMD55+ACMD41 with HCS bit set loop until ready; CMD58 READ_OCR checks CCS bit for SDHC block-addressed vs SDSC byte-addressed; switch to high-speed 12-25 MHz); read/write commands (CMD17 READ_SINGLE_BLOCK, CMD24 WRITE_BLOCK, CMD18 READ_MULTI_BLOCK, CMD25 WRITE_MULTI_BLOCK, CMD13 SEND_STATUS; SDHC addresses are block numbers, SDSC are byte addresses); C pseudocode for sd_init and sd_read_block with full sequence and start token 0xFE handling. **File system support**: FAT16 vs FAT32 vs exFAT (FAT32 on SDHC is standard; exFAT requires licensing rarely on MCU); FAT implementation (boot sector BPB parsing, FAT table cluster chain following, directory listing 8.3 + LFN, file operations); libraries (FatFs by Elm-Chan de facto standard supports FAT12/16/32 LFN partitions; Petit FatFs smaller read-only; Arduino SD library). **Spectrum integration**: **DivMMC** (most popular standard successor to DivIDE, plugs into expansion port, contains MCU + SD slot, presents 16 KB DivMMC ROM paged into address space, contains file browser **ESXOS** or **DIVI**, loads .tap/.tzx/.z80/.sna/.scr formats; **DivMMC EnJon** is popular commercial implementation with Kempston port); **ZXMMC** by Zaxos (open-source, Z80 bit-bangs SPI directly no MCU, needs CPLD/GAL for address decoding); **ZX Div Future OS** (complete operating environment for SD-connected Spectrums); tape emulation (simplest — MCU plays .tap/.tzx through Spectrum's EAR input, no expansion port needed, works with all models, loads at tape speed); generic mass storage (TR-DOS via [VG93 emulation](mcu_fdc_vg93.md) mapping .trd images, +3 DOS via uPD765 emulation mapping .dsk images). **Image file formats**: tape (.tap simplest raw blocks with sync/header/data/checksum, .tzx comprehensive supporting custom speed loaders and copy protection with different block types, .csw Compressed Square Wave sample-based, .wav raw audio); snapshots (.sna 48K snapshot with RAM + CPU registers PC/SP/AF/BC/DE/HL/IX/IY, .z80 flexible supporting 48K/128K with hardware state extensions, .sp/.szx/.rzx less common); disk images (.trd TR-DOS, .dsk generic, .fdi raw MFM); screen (.scr 6912 bytes = 6144 pixel + 768 attribute). **Existing projects**: DivMMC EnJon (commercial microSD + ESXOS + Kempston + tape passthrough), ZXMMC by Zaxos (open-source direct SPI), ZX-Uno and MiSTer (integrated SD via DivMMC), simple DIY adapters (Arduino + SD shield, RP2040 Pico + SD breakout, ESP32 with built-in SD), tape-only emulators (simplest connecting to EAR input). Comparison table of approaches (Tape emulator ~£2 easy all Spectrums; DivMMC ~£10-20 medium best all-rounder; ZXMMC ~£5 DIY hard minimalist; TR-DOS emulation ~£3 STM32 Pentagon owners; Integrated ~£50-150 for FPGA). FAQ (max SD card size 32 GB SDHC since SDXC exFAT rarely supported; SD card not recognised — wrong format or counterfeit; hot-swap supported via CD pin but risky during write; loading speed tape emulation ~1500 baud vs DivMMC near-instant vs ZXMMC 50-200 KB/s; saving supported with 100k writes/sector endurance; no special ROM needed for DivMMC but loader needed for ZXMMC; +3 DOS via uPD765 emulation less common than Beta 128). Summary (5 functions: connect SD via SPI, implement FAT file system, load various image formats, present storage to Spectrum as browser/TR-DOS/tape, provide fast loading). References (SD Physical Layer Simplified Spec, FatFs by Elm-Chan, DivMMC documentation, ZXMMC project, TAP/TZX file format specs by Tomaz Kac, SNA/Z80 specs, RP2040 SPI examples, Arduino SD library). Cross-references |
| 8 | [n_go.md](n_go.md) | Coming soon: N-Go — MCU-based Spectrum implementation |
| 9 | [mcu_design_patterns.md](mcu_design_patterns.md) | Coming soon: Design patterns — bus interfacing, level shifting, timing-critical I/O, GPIO speed requirements |

See [PLAN.md](../../PLAN.md) for the full article catalog.
