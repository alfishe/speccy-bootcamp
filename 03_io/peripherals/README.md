[← Home](../../README.md) · [Peripherals](README.md)

# I/O — Peripherals

This directory covers input devices, expansion interfaces, output hardware, and the broader peripheral ecosystem around the ZX Spectrum family — original Sinclair, Soviet clones, and New Generation hardware. Sound cards live in a dedicated subdirectory ([06_sound](../../06_sound/)); mass-storage peripherals live in [03_io/storage](../storage/README.md).

---

## Articles

| # | Article | Description | Lines |
|---|---------|------------|-------|
| 1 | [interface1.md](interface1.md) | **ZX Interface 1** — Sinclair's 1983 triple-function expansion (Microdrive controller + RS-232 + ZX Net LAN). 8 KB shadow ROM paging trick (`M1` fetch at `#0008`), 3-port I/O map (`#F7`/`#EF`/`#E7`), complete hook-code API (`#1B`–`#32`), ZX Microdrive sector format (254 sectors × 543 bytes, bespoke non-CRC checksum), bit-bang RS-232 (50–9600 baud), ZX Net single-wire open-collector token bus for 64 stations, system-variable layout at `#5CB6–#5CEF`, comparison with Opus Discovery / DISCiPLE / Beta Disk / +3, full BASIC syntax extension table | 570 |
| 2 | [interface2.md](interface2.md) | **ZX Interface 2** (Sinclair, 1983) — twin-joystick + ROM-cartridge expansion. MT62001 custom IC for joystick decode (port `#EFFE` = joy 1, `#F7FE` = joy 2, with bit-order swap), 28-pin cartridge socket pinout mirroring 27128 EPROM with `/ROMCS` pulled high to disable internal ROM, A14/A15/`/MREQ` decode for #0000-#3FFF cartridge overlay, the 10 released cartridges, +2A/+3 incompatibility and two-diode fix, the homebrew cartridge ecosystem | 347 |
| 3 | [multiface.md](multiface.md) | **Multiface (One / 128 / 3)** — Romantic Robot's hardware overlay peripheral (1986–1988). 8 KB ROM + 8 KB RAM paged in via NMI vector fetch at `#0066`, three model variants with distinct port maps (`#9F`/`#1F` for MF1, `#BF`/`#3F` for MF128, `#3F`/`#BF` for MF3), `+3` paging-port back doors (`#7F3F`/`#1F3F`), stealth mode flip-flop, dump-file format (precursor to `.z80`), Genie disassembler and Lifeguard poke-finder tool ecosystem, cultural impact on cheat codes and snapshots | 411 |
| 4 | [keyboard.md](keyboard.md) | **Keyboard Reading** — software-side companion to `keyboard_matrix.md`. Half-row scan idiom, full 40-key scan with bit-packing, “any-key” probe, ghosting mechanics and why QAOP/CS won, per-model differences (48K ULA port, 128K AY port B, +2A/+3 multi-stage scan, PS/2 via Next/DivMMC/Harlequin), debounce/auto-repeat/redefine patterns, `IN A,(n)` vs `IN A,(C)` trap, EAR bit interference, 10 pitfalls | 343 |
| 5 | [mouse.md](mouse.md) | **Mouse Interfaces** — Kempston Mouse (1985, 8-bit absolute position counters at `#FBDF`/`#FFDF`/`#FADF`, quadrature decode in hardware) vs AMX Mouse (Z80 PIO + diode decode, 1-bit relative polling at `#1F`/`#3F`/`#DF`, button polarity opposite, aliasing conflict with Kempston joystick), Kempston Mouse Turbo (2008 PS/2), K-MOUSE Turbo, ZX Next emulation, PS/2 protocol primer, 10 pitfalls | 371 |
| 6 | [joystick.md](joystick.md) | **Joystick Interfaces** — Kempston (`#1F`), Sinclair/Interface 2 (matrix rows `#EFFE`/`#F7FE`), Cursor/Protek/AGF, Fuller (`#7F`), Timex (TS2068 AY reg 14). Active-high vs active-low polarity, decoding variants, Beta 128 conflict, unified table-driven reader, decision guide | 399 |
| 7 | [printers.md](printers.md) | **Printers** — ZX Printer (1981, spark / electro-erosion technology, port `#FB` with `A2=0` decode, paper-start latch in bit 7 read, next-pixel latch in bit 0, stylus/motor/speed bits on write, +9V power from edge connector — removed on +2A/+3), Alphacom 32 (thermal-paper self-powered alternative), Centronics adapters (Kempston/DK'Tronics convention: data `#0F`, status `#1F`, hardware-strobe on write), Soviet SM640 (IEEE 488/IEC 625) and SM646 (Centronics, GOST 19768-74 Cyrillic), Retro-Printer modern emulation, 10 pitfalls | 367 |
| 8 | [zx_bus.md](zx_bus.md) | **ZX Bus** — the 56-pin (28+28) expansion edge connector: full pinout table for all models, signal groups (A0-A15 address, D0-D7 data, `/MREQ`/`/IORQ`/`/RD`/`/WR`/`/M1`/`/RFSH` control, `/INT`/`/NMI`/`/WAIT`/`/HALT`/`/RESET`/`/BUSRQ`/`/BUSACK` CPU state, `/ROMCS` vs `/ROM1OE`+`/ROM2OE` per generation, `CK`, `/IORQULA`, power rails), per-model differences (16K/48K vs 128K/+2 vs +2A/+3 with +9V/`-5V` removal and `/ROMCS`→`/ROM1OE`+`/ROM2OE` change), `/ROMCS` overlay trick, no `/RAMCS`, `M1`-triggered overlays (Interface 1 at `#0008`, Multiface at `#0066`), DMA via `/BUSRQ`/`/BUSACK`, peripheral stacking order, 12 pitfalls | 405 |
| 9 | [mb02.md](mb02.md) | **MB-02 / MB-02+** — Czech all-in-one disk/DMA/memory/RTC/IDE expansion (8BC group, ~1996; ~90 units made). WD2797 FDC supports HD floppies (1.4-1.8 MB), Z80-DMA (RFT U858D required — Zilog original rejected by BS-DOS init-order bug), 128K-512K battery-backed SRAM, RTC-72421 real-time clock, 8K boot EPROM, optional IDE (MB-02+IDE), Kempston joystick/mouse/Centronics on-board, full I/O port map (`#01DF`-`#1FDF` range, paging at `#0FDF`), BS-DOS boot sequence and filesystem, Hood's NMI menu, cable-length signal integrity, MB03+ Ultimate FPGA successor | 413 |
| 10 | [video_output.md](video_output.md) | **Video Output** — physical video output stage of every Spectrum model. 48K RF-only path (ULA Y/U/V → LM1889 → RF modulator on UHF ch.35/36) vs 128K/+2 TEA2000-based RGB+composite path vs +2A/+3 Amstrad 40077 gate-array path with RGB only; 8-pin DIN 45326 monitor socket pinouts for all three variants (128K TTL RGB vs +2 configurable via LK1-LK8 jumpers vs +2A/+3 with `+12V` on pins 1/5 and audio on 3); composite video vs composite sync distinction; SCART cable wiring tables (128K needs diode Bright mixing; +2 internal mixing; +2A/+3 requires 330 Ω series R for proper levels); 48K composite mod (LM1889 tap + 2N3904 buffer); S-Video alternative (Redhawk PCB); VGA/HDMI adapters (GBS-8200 with gbscontrol, OSSC, RGB-to-HDMI, ZX-VGA-JOY); Soviet clone composite outputs; 12 pitfalls (most common: +12V injected into TV input when 128K cable plugged into +2A/+3) | 413 |
| 11 | [z_controller.md](z_controller.md) | **Z-Controller** — Russian multi-I/O peripheral designed by Alexey Zhabin (KingOfEvil) in 2007. Combines PS/2 keyboard, PS/2 mouse (Kempston Mouse emulation at `#FBDF`/`#FFDF`/`#FADF`), Nemo IDE-compatible CompactFlash/hard disk, and SPI-mode SD card on a single ZX Bus board. Hardware uses a two-chip architecture: Altera EPM7128 CPLD (128 macrocells, fast port-decoded logic) handles mouse register emulation and IDE latch; KR1878VE1 microcontroller handles PS/2 scan-code conversion. Not natively bootable from SD (driver must be loaded from floppy or IDE first). Software ecosystem is Russian-scene-specific (Wild Disk Copier v1.21+, iS-DOS patches); not compatible with esxDOS. Functionality carried forward into MB03+ Ultimate FPGA and ZX Spectrum Neo | 333 |
| 12 | [lightgun.md](lightgun.md) | **Magnum Light Phaser** — Amstrad's 1987 light gun, the last first-party ZX Spectrum peripheral. Photo-diode in the muzzle detects the CRT raster beam's bright pulse; trigger microswitch signals fire. Three interface variants: +2/+2A/+2B/+3/+3B AUX port (8-pin connector), 48K/128K Toastrack ZX Bus edge connector + MIC socket interface box, C64 user port + control port (the last uses VIC-II hardware light-pen input `$D013`/`$D014`). The ZX Spectrum ULA has **no light-pen position register**, so detection is entirely software-driven: the sensor pulse drives the ULA's `/INT` line, software counts T-states since VSync, and a per-model calibration table (48K / 128K / +2 / +2A / +3) converts the count to (x, y). Around 15 known compatible Spectrum titles (6 bundled: Bullseye, Missile: Ground Zero, Operation Wolf, Robot Attack, Rookie, Solar Invasion; 9 separately-sold: Billy the Kid, Bronx Street Cop, etc.). Rebranded variants (Trojan Phazer white plastic, Cheetah Defender, MARPES) are interchangeable; Stack Light Rifle is incompatible (emulates a Kempston joystick). **CRT-only — does not work on LCD/plasma/OLED**; Sinden Lightgun and GUN4IR are the modern IR-LED-bar alternatives. 12 pitfalls | 306 |

See [PLAN.md](../../PLAN.md) for the full article catalog. All articles complete.
